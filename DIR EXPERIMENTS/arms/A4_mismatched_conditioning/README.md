# A4 — Mismatched conditioning (optional)

Synthesizer conditioned on a **different** patient’s static CT. Specificity control for A3.

KPI: **75-point** TRE only (`--set 75`).

Status: optional / later.
