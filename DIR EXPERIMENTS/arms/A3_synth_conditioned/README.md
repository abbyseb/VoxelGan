# A3 — Synthesised 4D-CT VoxelMap (**main**)

On-table / mid-phase CT → conditioned synthesizer (e.g. G160-A1 Decoder + µ) →
synthetic 4D → DRR → VoxelMap → TRE on DIR landmarks.

Compares against A0–A2 (identity, patient oracle, DIR population).

Status: scaffold — mirror `VoxelMap_Experiments` synth arm on DIR cases.
