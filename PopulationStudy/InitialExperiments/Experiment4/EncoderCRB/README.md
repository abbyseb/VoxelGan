# Experiment 4 — Encoder CRB

**Code ready, not started.** Frozen anatomy side-branch concatenated onto `[t_ref, t_tgt]`.

To run later:

```bash
PYTHONPATH=.. PYTHONUNBUFFERED=1 python train_crb_enc_mse.py --gpu 0
```
