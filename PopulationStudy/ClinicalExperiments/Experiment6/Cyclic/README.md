# Experiment 6 — Cyclic (128³)

Cyclic phase encoding (`cond_dim=4`), train on **full 128³** volumes.

**Not started yet** — scaffold only. Run after Normal section is underway / done.

Will mirror `../Normal/` train script with Experiment2 cyclic nets.
